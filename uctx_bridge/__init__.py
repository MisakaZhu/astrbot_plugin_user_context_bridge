"""uctx_bridge：同一用户跨会话上下文共享插件的核心模块。

模块划分（ADR-002/006）：
- identity：共享身份键（平台实例 + 机器人 self_id + 人格范围 + 真实发送者）。
- scope：来源范围与归属判定（显式启用、群白名单/私聊开关、个人退出、
  排除未唤醒闲聊、机器人自言与管理命令）。
- ledger（P2）：SQLite 轮次账本，唯一权威历史源。
- lifecycle（P3/P4）：轮次生命周期协调与并发控制。
"""
