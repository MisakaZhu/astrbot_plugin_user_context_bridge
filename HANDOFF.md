# HANDOFF（交接说明）

面向 Codex 独立验收（MIS-144）与用户实机验证（MIS-145）。随阶段推进更新；最终以 P7 提交为准。

## 当前状态

- 阶段：P0 完成（待复核），P1 进行中。
- 最新提交：见 docs/STATUS.md「阶段历史」。
- 工作区：干净（无未提交源码改动）。

## 关键决策入口

- 宿主调用链事实与实证：docs/ADR.md ADR-001；tests/p0_lifecycle_check.py（17 项断言，双宿主版本）。
- 唯一权威历史来源与写回短路：ADR-002。
- 轮次终态机三轨（on_agent_done 主轨 + on_decorating_result 兜底轨 + 重启恢复轨）：ADR-003。**注意 V4/V5 实证**：模型 err 两版均不触发完成钩子；aborted 回调形态版本各异且旗标滞后——兜底轨是必须项而非优化项。
- 并发/epoch/身份/范围：ADR-004 ~ ADR-006。

## 验收建议（Codex）

1. 复跑 P0 验证（两版宿主 venv）核对 17/17。
2. 重点核查 ADR-002 的写回短路是否有未覆盖的宿主路径（如 webchat 分支、third_party 执行器）——v1 声明均不支持。
3. 后续阶段按 A01-A18 映射（docs/ACCEPTANCE.md，P6 建立）抽查断言是否落在「最终模型请求 / 实际持久化 / 回复目标」三处真实证据上。

## 待用户决定

- 新代码开源许可（发布前）。
- GitHub 仓库名称、远端与上传（P7 后另行决定，本仓库无 remote）。
- 真实 QQ 测试环境演练（MIS-145）。

## 已知限制（随阶段更新）

- v1 不自动迁移既有 QQ 历史（从启用后的有效对话开始积累）。
- 同库双 AstrBot 实例并发写不做支持（启动租约检测 + 拒绝）。
- 原生 /new、/reset 与共享 epoch 的同步语义依赖 P5 验证结果，若宿主接口不足将列为阻塞项。
